# NPM Configuration File

# Utiliser npm 9+ (pour meilleure performance)
# Installer les dépendances avec un comportement déterministe
legacy-peer-deps=false

# Éviter les permissions issues
unsafe-perm=true
